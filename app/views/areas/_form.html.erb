<%= semantic_form_for @area, :html => { :class => "form_mobile", "data-ajax" => false } do |f| %>
    <%= f.input :description, :label => t("description").capitalize %>
    <%= form_buttons areas_path, @area, f %>
<% end %>
